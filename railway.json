{
  "version": 2,
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn simple_api:app --host 0.0.0.0 --port $PORT --workers 1",
    "healthcheckPath": "/health"
  }
}
